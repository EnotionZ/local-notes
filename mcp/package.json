{
  "name": "local-notes-mcp",
  "version": "1.0.0",
  "description": "MCP server for local-notes — lets LLMs read, search, create, and update markdown notes",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --import tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.16.0",
    "zod": "^3.24.4"
  }
}
